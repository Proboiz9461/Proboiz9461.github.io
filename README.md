# Shaurya's Creations

Recreated from scratch as standalone app bundles:

- 50 tools under `apps/tools/tool-XX/`
- 50 games under `apps/games/game-XX/`
- each app has `index.html`, `style.css`, and `script.js`
- total app files: **300**

## Run

```bash
python3 -m http.server 8000
```
