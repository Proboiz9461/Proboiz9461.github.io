const area=document.getElementById('gameArea');const statusEl=document.getElementById('status');const scorePill=document.getElementById('scorePill');const roundPill=document.getElementById('roundPill');let score=0,round=0;const sync=()=>{scorePill.textContent=`Score: ${score}`;roundPill.textContent=`Round: ${round}`};let cpu=0,you=0;area.innerHTML='<p>Race to 20 points. Roll your die each round.</p><div class="row"><button id="roll">Roll Dice</button><button id="reset">Reset Race</button></div>';const roll=area.querySelector('#roll');const reset=area.querySelector('#reset');function fresh(){round=0;you=0;cpu=0;statusEl.textContent='Race reset. First to 20 wins!';sync()}roll.onclick=()=>{round++;you+=1+Math.floor(Math.random()*6);cpu+=1+Math.floor(Math.random()*6);statusEl.textContent=`You ${you} vs CPU ${cpu}`;if(you>=20||cpu>=20){if(you>=20&&you>=cpu){score+=5;statusEl.textContent+= ' · You win race! +5';}else{statusEl.textContent+= ' · CPU wins race!';}you=0;cpu=0;}sync();};reset.onclick=fresh;fresh();