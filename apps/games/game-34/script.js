const area=document.getElementById('gameArea');const statusEl=document.getElementById('status');const scorePill=document.getElementById('scorePill');const roundPill=document.getElementById('roundPill');let score=0,round=0;const sync=()=>{scorePill.textContent=`Score: ${score}`;roundPill.textContent=`Round: ${round}`};const colors=['Red','Blue','Green','Yellow'];area.innerHTML='<p>Memorize the sequence then type it back.</p><div class="row"><button id="show">Show Sequence</button><input id="ans" placeholder="e.g. Red Blue Green"><button id="check">Check</button></div>';const show=area.querySelector('#show'),ans=area.querySelector('#ans'),check=area.querySelector('#check');let seq=[];show.onclick=()=>{round++;seq=Array.from({length:3+Math.floor(Math.random()*2)},()=>colors[Math.floor(Math.random()*colors.length)]);statusEl.textContent=`Remember: ${seq.join(' - ')}`;setTimeout(()=>statusEl.textContent='Now type the sequence with spaces.',1800);sync();};check.onclick=()=>{const ok=ans.value.trim().toLowerCase()===seq.join(' ').toLowerCase();if(ok){score+=4;statusEl.textContent='Awesome memory! +4';}else{statusEl.textContent=`Not quite. Correct: ${seq.join(' ')}`;}sync();};sync();