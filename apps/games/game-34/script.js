let s=0;const app=document.getElementById('app'),out=document.getElementById('out');const b=document.createElement('button');b.textContent='Tap Fast';app.append(b);let t=0;b.onclick=()=>{if(!t){t=Date.now();out.textContent='Tap again now!';}else{const ms=Date.now()-t;t=0;const p=Math.max(1,Math.round((700-ms)/70));s+=p;out.textContent=`${ms}ms | +${p} | Score: ${s}`;}};