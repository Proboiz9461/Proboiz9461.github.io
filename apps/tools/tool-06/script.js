const app=document.getElementById('app'),out=document.getElementById('out');const t=document.createElement('input'),btn=document.createElement('button');t.placeholder='Text';btn.textContent='Count';app.append(t,btn);btn.onclick=()=>{const v=t.value.trim();out.textContent=`Chars ${v.length} | Words ${v?v.split(/\s+/).length:0}`};