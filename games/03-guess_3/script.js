const game=document.getElementById('game');const out=document.getElementById('out');
let ans=1+Math.floor(Math.random()*20),left=6;game.innerHTML='<input id=n type=number placeholder=1-20><button id=g>Guess</button><button id=r>Restart</button>';g.onclick=()=>{let v=+n.value;if(!v)return out.textContent='Enter a number.';left--;if(v===ans)out.textContent='🎉 Correct!';else if(left<=0)out.textContent=`💥 Number was ${ans}`;else out.textContent=v<ans?`Too low (${left} left)`:`Too high (${left} left)`};r.onclick=()=>{ans=1+Math.floor(Math.random()*20);left=6;n.value='';out.textContent='New round.'};