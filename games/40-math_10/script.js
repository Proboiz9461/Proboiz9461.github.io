const game=document.getElementById('game');const out=document.getElementById('out');
let a,b,op,score=0,round=0;game.innerHTML='<button id=s>Start</button><input id=ans type=number placeholder=Answer><button id=sub>Submit</button>';const next=()=>{a=1+Math.floor(Math.random()*20);b=1+Math.floor(Math.random()*20);op=['+','-','*'][Math.floor(Math.random()*3)];out.textContent=`Q${round+1}: ${a} ${op} ${b} = ?`};s.onclick=()=>{score=0;round=0;next()};sub.onclick=()=>{if(round>=5)return;let right=op==='+'?a+b:op==='-'?a-b:a*b;if(+ans.value===right)score++;ans.value='';round++;if(round>=5)out.textContent=`🧠 Score ${score}/5`;else next()};