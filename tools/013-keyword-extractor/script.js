const d=document.createElement('div');d.className='cursor-dot';const r=document.createElement('div');r.className='cursor-ring';document.body.append(d,r);addEventListener('mousemove',e=>{d.style.left=r.style.left=e.clientX+'px';d.style.top=r.style.top=e.clientY+'px';});const $=s=>document.querySelector(s);const out=t=>$('#out').textContent=t;const val=id=>document.getElementById(id)?.value||'';document.getElementById('run').onclick=()=>{const stop=new Set(['the','and','to','of','in','a','is','for','on','with']);const words=val('t').toLowerCase().match(/[a-z0-9']+/g)||[];const m={};for(const w of words){if(w.length<3||stop.has(w))continue;m[w]=(m[w]||0)+1;}out(Object.entries(m).sort((a,b)=>b[1]-a[1]).slice(0,10).map(([k,v])=>`${k} (${v})`).join('\n'));};