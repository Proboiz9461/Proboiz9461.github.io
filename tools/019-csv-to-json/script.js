const d=document.createElement('div');d.className='cursor-dot';const r=document.createElement('div');r.className='cursor-ring';document.body.append(d,r);addEventListener('mousemove',e=>{d.style.left=r.style.left=e.clientX+'px';d.style.top=r.style.top=e.clientY+'px';});const $=s=>document.querySelector(s);const out=t=>$('#out').textContent=t;const val=id=>document.getElementById(id)?.value||'';document.getElementById('run').onclick=()=>{const lines=val('t').trim().split(/\n+/);if(lines.length<2)return out('Need header + rows');const h=lines[0].split(',').map(x=>x.trim());const arr=lines.slice(1).map(r=>{const v=r.split(',');let o={};h.forEach((k,i)=>o[k]=(v[i]||'').trim());return o;});out(JSON.stringify(arr,null,2));};