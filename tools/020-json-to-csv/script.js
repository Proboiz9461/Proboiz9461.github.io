const d=document.createElement('div');d.className='cursor-dot';const r=document.createElement('div');r.className='cursor-ring';document.body.append(d,r);addEventListener('mousemove',e=>{d.style.left=r.style.left=e.clientX+'px';d.style.top=r.style.top=e.clientY+'px';});const $=s=>document.querySelector(s);const out=t=>$('#out').textContent=t;const val=id=>document.getElementById(id)?.value||'';document.getElementById('run').onclick=()=>{try{const arr=JSON.parse(val('t'));if(!Array.isArray(arr)||!arr.length)return out('Need JSON array');const h=Object.keys(arr[0]);const rows=[h.join(',')];for(const o of arr)rows.push(h.map(k=>String(o[k]??'')).join(','));out(rows.join('\n'));}catch{out('Invalid JSON');}};